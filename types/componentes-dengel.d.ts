declare module 'componentes-dengel' {
    // Aquí puedes declarar las propiedades y métodos que están disponibles en el módulo 'componentes-dengel'
    // Por ejemplo, si el módulo tiene una propiedad 'version' y un método 'printVersion', podrías escribir:
    const HeaderMobileTypeOne,NavMobileTypeOne: string;
    function printVersion(): void;
  }